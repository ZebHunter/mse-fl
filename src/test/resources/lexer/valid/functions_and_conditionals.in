fun compute(a: Int, b: Float, flag: Bool): Float {
    let sum: Float = b + 10.5;
    if (flag == true) {
        sum = sum * a;
    } else {
        sum = sum / 2.0;
    }
    sum;
}
let ok: Bool = false;
let msg: String = "done";
compute(3, 1.5, ok);
